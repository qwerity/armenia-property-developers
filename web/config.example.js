// Copy to config.js and fill in your key, or let build.py generate it from .env / the environment.
// config.js is git-ignored: the key is per-deployment and must stay out of the repository.
window.APP_CONFIG = { googleMapsApiKey: "YOUR_GOOGLE_MAPS_API_KEY", googleMapId: "DEMO_MAP_ID" };
